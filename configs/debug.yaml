experiment:
  method: RE
  mode: debug
  search_seed: 1001

dataset:
  name: CIFAR10
  split_seed: 20260823
  train_size: 20000
  val_size: 5000
  num_workers: 2

training:
  epochs: 1
  batch_size: 128
  optimizer: SGD
  learning_rate: 0.05
  momentum: 0.9
  weight_decay: 0.0005
  deterministic: true

evolution:
  population_size: 4
  tournament_size: 2
  budget: 8

device:
  use_cuda: true
